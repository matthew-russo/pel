func main() {
  let some_string: Maybe<<string>> := Maybe<<string>>::Some("hello");
  let nunya: Maybe<<string>> := Maybe<<string>>::None;
  print("hello world");
}

enum Maybe<<T>> {
  variants {
    Some(T),
    None,
  }
}

